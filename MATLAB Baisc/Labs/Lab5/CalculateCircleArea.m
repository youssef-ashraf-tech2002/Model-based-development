% Create a function named calculateCircleArea
% that takes the radius of a circle as input and returns the area of the circle.
% Call the function with different radii and display the results.

function area = CalculateCircleArea(radius)
    area =(radius)^2 *pi  ;
    disp('area = ')
    disp(area)
end